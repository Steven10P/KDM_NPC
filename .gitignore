# NUNCA subir credenciales
github_token.txt
*.token
.env

# Python / notebooks
__pycache__/
*.pyc
.ipynb_checkpoints/
venv/
.venv/

# Datos pesados (descargar con scripts, no versionar)
# (anclados a la raíz: src/data/ SÍ se versiona)
/data/
/datasets/
*.zip
*.tar.gz

# Repos de referencia clonados (no versionar dentro de este repo)
external/

# Experimentos y credenciales de servicios
wandb/
kaggle.json
kaggle_token.txt
mlflow.db
mlruns/

# Descargas crudas de logs de Kaggle (diagnóstico ad-hoc, no son metrics.json)
experiments/*/results/_kaggle_output_*/

# Checkpoints pesados (van a Drive, no a git) — solo metrics.json/git_commit.txt se versionan
experiments/*/results/*/checkpoints/

# Sistema
Thumbs.db
.DS_Store

# Repos git anidados dentro de skills (no versionables como gitlinks)
.claude/skills/embedding-kit/
.claude/skills/ml-experiment-skills/
